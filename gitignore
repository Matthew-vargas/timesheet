__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.venv
*.db
*.sqlite
.DS_Store

# Database file — production data lives on Render's persistent disk,
# not in the repo. Exclude to prevent stale data from being deployed.
timesheets_database.json
